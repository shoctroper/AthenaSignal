# AthenaSignal M3 — Autonomous Signal Intelligence (bootstrap / authorization run).
# Deterministic gates are VERDE over the post-M2 repository, so the root run
# yields a HUMAN GATE via required semantic review (offline reviewer
# unavailable). The human RETURN authorizes the OpenCode worker to implement
# ciclo/ORDEN-007.md. Functional verification is done by
# athenasignal-m3-verify.spec.yaml.
id: athenasignal-m3-autonomous-signal-intelligence
version: 1

root: ../..

gate_policy:
  scope:
    required: true
  tests:
    required: true
  artifacts:
    required: true
  regressions:
    required: true
  reproducibility:
    required: true

review:
  enabled: true
  required: true

scope:
  baseline: governance/specs/athenasignal-m3.baseline.json
  allow:
    - src/
    - tests/
    - scripts/
    - ciclo/
    - docs/
    - evidence/
    - governance/
    - package.json
    - package-lock.json
    - tsconfig.json
    - AGENTS.md
    - README.md
  deny:
    - decisiones/
    - .env
    - perfil_navegador_llm/
  ignore:
    - governance
    - node_modules
    - perfil_navegador_llm

acceptance:
  tests:
    command: npm test
    required: true
    junit: false
    timeout: 300.0

  regressions:
    max: 0
    baseline: governance/specs/athenasignal-m3.regressions.json

  artifacts:
    required:
      - path: src/domain/entities.ts
        kind: file
      - path: src/research/ResearchPipeline.ts
        kind: file
      - path: package.json
        kind: file

verification:
  reproducible_runs: 2
  reproducible_command: >-
    node -e "process.stdout.write('athenasignal-m3-probe')"
  reproducible_timeout: 60.0

escalate_when:
  - ambiguous_specification
